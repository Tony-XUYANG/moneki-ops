# DEMO

建议录屏时按以下顺序操作，约 90 秒即可完成。

## 1. 哪个品类的门店营业额最高？

在全量日期范围提问：

```text
哪个品类的门店营业额最高？
```

回答：**日料，¥88,718.00，共 2,423 单**。

证据链：`query_store_category_revenue` → `sales_clean JOIN stores` → 按门店 `category` 聚合。点击“同步到看板”会把明细切换到门店对比。

## 2. 牛肉poke 六月卖了多少钱？

回答：**2026-06-01 至 2026-06-30，牛肉poke 营业额 ¥13,524.00，322 份，182 单**。

证据链：商品名称先在 `products` 维表解析为 `P06`，再由 `query_product_revenue` 查询。看板 6 月 Top 10 商品第一名也显示 ¥13,524.00。

继续问：

```text
那五月呢？
```

回答会继承牛肉poke，只将月份切换为 5 月，并重新查询为 **¥13,104.00**。这验证了上下文追问不是把上一轮文字复制过来。

## 3. 客单价最近是涨了还是跌了？

回答：**7 月 ¥36.05，相比 6 月 ¥35.18 上涨 ¥0.87（2.47%）**。

证据链：`query_monthly_aov` 返回 5 月、6 月、7 月每月营业额和去重订单数，程序用 Decimal 计算方向；AI 只负责问题路由。

## 录屏检查点

- 顶部日期、门店筛选可以刷新看板。
- 质量弹窗显示 12,131 原始行、11,823 有效行、308 隔离行及拒绝原因。
- AI 回答下方出现“已核验”和“同步到看板”。
- 问“天气怎么样？”会返回不可回答说明，不会编造数字。

