# AI_USAGE

## 使用的工具与任务拆分

本次实现使用 Codex 作为代码协作工具，并使用浏览器查看公开题目仓库和验证本地页面。任务拆成：

1. 阅读 README，列出三关、硬性要求和交付文件。
2. 分析三份 CSV，确定可复现的清洗规则和指标基线。
3. 实现原始层、有效层、拒绝层和导入审计。
4. 实现统一指标查询服务，确保看板和 AI 不走两套口径。
5. 实现受控 AI 路由、数据库工具、上下文追问、兜底和 SSE。
6. 实现看板、门店对比、数据质量弹窗和图表联动。
7. 用回归测试验证 AI evidence 数字等于查询结果。

## 真实 prompt 示例

```text
请先读取任务 README 和三份 CSV，统计原始行数、日期格式、重复、缺失、非法金额、
非正数量以及不存在的 store_id/product_id。不要修改数据，输出每一类数量和建议的
清洗口径。金额里的 ¥ 是格式问题，不能直接当作非法数据。
```

## AI 出错、发现与修复

早期分析脚本只接受 `YYYY-MM-DD` 和 `YYYY/MM/DD`，因此把 75 条 `DD-MM-YYYY` 日期误判为非法；同时把带 `¥` 前缀的 40 条金额当作不可解析。通过逐行查看异常值并用人工可解释的规范化函数复核后修正：日期增加 `DD-MM-YYYY` 解析，金额在 Decimal 转换前去除货币符号。修复后有效行从 11,783 变为 11,823，且测试固定断言这个结果。

另一个风险是让模型直接回答金额。实现中没有采用这个方案：模型只能路由到 `query_product_revenue`、`query_store_category_revenue`、`query_monthly_aov` 等白名单工具，数字由 `MetricsService` 查询并由程序模板拼接。测试直接比较 `evidence.value` 和同一查询层的结果，避免语言模型改写数字。

## 自己编写的部分

数据清洗规则、整数分金额、SQLite schema、SQL 指标查询、AI 工具白名单、会话状态、SSE 协议、图表 SVG、数据质量界面和测试由我自己决定并实现。原因是这些部分涉及题目特定的数据口径和可信性，不能交给模型凭经验猜测。

